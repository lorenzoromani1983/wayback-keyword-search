{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd/downloader/main.go",
            "args": [
                "--domain=YOUR_SITE",
                "--timeStamp=2022",
                "--workers=15",
            ],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}
