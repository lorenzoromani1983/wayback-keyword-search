{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd/download_channels/download_channels.go",
            "args": [
                "--domain=YOUR_SITE",
                "--timeStamp=2023",
                "--workers=15",
            ],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}
